const nome = ["ana Julia", "Caio vinicius", "BIA silva"];

// const nomesPadronizados = nome.map((nome)=>nome.toUpperCase());

// console.log(nomesPadronizados)

nome.forEach((nome)=>{console.log(nome.toUpperCase())})

console.log(nome)